services:
  jekyll-site:
    image: jekyll-site
    build: .

    volumes:
      - .:/usr/src/app

    ports:
      - "4000:4000"
      - "35729:35729"

    user: "1000:1000"

    environment:
      - JEKYLL_ENV=docker

    develop:
      watch:
        - action: rebuild
          path: ./Dockerfile

        - action: rebuild
          path: ./Gemfile

        - action: rebuild
          path: ./Gemfile.lock